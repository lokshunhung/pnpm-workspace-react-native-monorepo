// @ts-check
"use strict";

/** @type {import("prettier").Options} */
const config = {
    arrowParens: "avoid",
    printWidth: 160,
    tabWidth: 4,
    trailingComma: "all",
};

module.exports = config;
